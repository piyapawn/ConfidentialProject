// Click Sound
const clickSource = "/Assets/PictureAndVdo/เสียง_ในส่วนของดัง/สำเนาของ เสียงกด.mp3"
const clickSound = new Audio(clickSource);

function clickSoundPlay() {
    clickSound.play()
}

// Accept Sound
const acceptSource = "/Assets/PictureAndVdo/เสียง_ในส่วนของดัง/สำเนาของ กดaccept.mp3"
const acceptSound = new Audio(acceptSource);

function acceptSoundPlay() {
    acceptSound.play()
}

// Intro Sound
const introTextSource = "/Assets/PictureAndVdo/เสียง_ในส่วนของดัง/สำเนาของ ข้อความintro.mp3"
const introTextSound = new Audio(introTextSource);

function introTextSoundPlay() {
    introTextSound.play()
}